#include <stdio.h>

int main() {
    // declaring an int array named numbers
    int numbers[5];
    
    // inserting values for first & last element
    
    numbers[0]=10;
    numbers[4]=50;
    // printing values
    printf("%d \n", numbers[0]);
    printf("%d \n ", numbers[4]);
    
    return 0;

}