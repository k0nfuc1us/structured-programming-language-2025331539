// Include the custom header file "calc.h" where the functions were declared[cite: 12]
#include "calc.h"

// Function definition to add two integers[cite: 12]
int add(int x, int y) {
    return x + y; // Returns the sum of x and y[cite: 12]
}

// Function definition to subtract one integer from another[cite: 12]
int subtract(int x, int y) {
    return x - y; // Returns the result of x minus y[cite: 12]
}