// Start by creating a header file named calc.h to declare the functions[cite: 11]

// Include guard checks if CALC_H is not defined to prevent multiple inclusions[cite: 11]
#ifndef CALC_H
// Define CALC_H so subsequent inclusions are skipped[cite: 11]
#define CALC_H

// Function declaration for an addition operation taking two integers[cite: 11]
int add(int x, int y);

// Function declaration for a subtraction operation taking two integers[cite: 11]
int subtract(int x, int y);

// End of the include guard[cite: 11]
#endif